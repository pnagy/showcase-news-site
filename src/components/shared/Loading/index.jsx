import React from 'react'
import styled from '@emotion/styled'

const LoadingText = styled('div')`
  font-size: 32px;
`

const Loading = () => <LoadingText>One sec, it's loading...</LoadingText>

export default Loading
