import React from 'react'
import styled from '@emotion/styled'

const LoadingText = styled('div')`
  font-size: 32px;
`

const Loading = () => (
  <LoadingText>Sorry! Couldn't load any articles... :(((( </LoadingText>
)

export default Loading
